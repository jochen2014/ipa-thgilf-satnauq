import * as flightController from './flightController';

describe("flight-controller", () => {
    it("should handle empty array", () => {
        // todo: mock req, res, next
        // verify all negative cases are handled properly
        expect(true).toBe(true);
    });
});
